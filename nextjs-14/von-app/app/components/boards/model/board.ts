export interface IBoard{
    id? : number,
    articles? : number,
    boardName? : string,
    boardType? : string,
    postdate? : string,
    modDate? : string,
    json? : IBoard,
    array?: [],

    count?:number,
}